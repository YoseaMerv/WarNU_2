<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Grup menu utama -->
    <group android:id="@+id/group_main" android:checkableBehavior="single">
        <!-- Item Status Aktif dengan ikon switch kustom -->
        <item
            android:id="@+id/nav_status"
            android:title="Status Aktif"
            android:icon="@drawable/custom_switch_selector"
            android:checkable="true" />
        <item
            android:id="@+id/nav_edit_profile"
            android:icon="@drawable/ic_profile"
            android:title="Edit Profil" />
        <item
            android:id="@+id/nav_history"
            android:icon="@drawable/ic_history"
            android:title="Riwayat Pengantaran" />
        <item
            android:id="@+id/nav_settings"
            android:icon="@drawable/ic_settings"
            android:title="Pengaturan" />
    </group>

    <!-- Grup untuk Logout agar terpisah -->
    <item android:title="">
        <menu>
            <item
                android:id="@+id/nav_logout"
                android:icon="@drawable/ic_logout"
                android:title="Log Out" />
        </menu>
    </item>

</menu>
